return {
  'mfussenegger/nvim-dap',
  config = function()
    local dap = require 'dap'
    -- Optional: Add any dap-specific configurations here.
    -- Example:
    -- dap.adapters.<language> = { ... }
    -- dap.configurations.<language> = { ... }
  end,
}
